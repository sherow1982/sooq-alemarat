{
  "name": "sooq-alemarat-cms",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"serve .\"",
    "build": "tinacms build && npm run build:site",
    "build:site": "mkdir -p dist && cp -r *.html assets css js images data legal pages dist/ && cp manifest.json robots.txt sitemap.xml google*.html CNAME dist/",
    "serve": "serve ."
  },
  "dependencies": {
    "tinacms": "^1.5.0",
    "@tinacms/cli": "^1.5.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}